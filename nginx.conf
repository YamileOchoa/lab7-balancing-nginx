events {}

http {
    upstream backends {
        server backend1:8081;
        server backend2:8082;
        server backend3:8083;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backends;
        }
    }
}